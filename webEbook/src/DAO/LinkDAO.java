package DAO;

import java.util.logging.Logger;

//insert link dao when generate link downlaod for buyer
public class LinkDAO {
	private static final Logger logger = Logger.getLogger(UserDAO.class.getName());
}
